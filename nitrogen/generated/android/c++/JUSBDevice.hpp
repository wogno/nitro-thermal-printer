///
/// JUSBDevice.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#include <fbjni/fbjni.h>
#include "USBDevice.hpp"

#include <optional>
#include <string>

namespace margelo::nitro::thermalprinter {

  using namespace facebook;

  /**
   * The C++ JNI bridge between the C++ struct "USBDevice" and the the Kotlin data class "USBDevice".
   */
  struct JUSBDevice final: public jni::JavaClass<JUSBDevice> {
  public:
    static auto constexpr kJavaDescriptor = "Lcom/margelo/nitro/com/thermalprinter/USBDevice;";

  public:
    /**
     * Convert this Java/Kotlin-based struct to the C++ struct USBDevice by copying all values to C++.
     */
    [[maybe_unused]]
    [[nodiscard]]
    USBDevice toCpp() const {
      static const auto clazz = javaClassStatic();
      static const auto fieldDeviceName = clazz->getField<jni::JString>("deviceName");
      jni::local_ref<jni::JString> deviceName = this->getFieldValue(fieldDeviceName);
      static const auto fieldVendorId = clazz->getField<double>("vendorId");
      double vendorId = this->getFieldValue(fieldVendorId);
      static const auto fieldProductId = clazz->getField<double>("productId");
      double productId = this->getFieldValue(fieldProductId);
      static const auto fieldDeviceId = clazz->getField<jni::JDouble>("deviceId");
      jni::local_ref<jni::JDouble> deviceId = this->getFieldValue(fieldDeviceId);
      return USBDevice(
        deviceName->toStdString(),
        vendorId,
        productId,
        deviceId != nullptr ? std::make_optional(deviceId->value()) : std::nullopt
      );
    }

  public:
    /**
     * Create a Java/Kotlin-based struct by copying all values from the given C++ struct to Java.
     */
    [[maybe_unused]]
    static jni::local_ref<JUSBDevice::javaobject> fromCpp(const USBDevice& value) {
      using JSignature = JUSBDevice(jni::alias_ref<jni::JString>, double, double, jni::alias_ref<jni::JDouble>);
      static const auto clazz = javaClassStatic();
      static const auto create = clazz->getStaticMethod<JSignature>("fromCpp");
      return create(
        clazz,
        jni::make_jstring(value.deviceName),
        value.vendorId,
        value.productId,
        value.deviceId.has_value() ? jni::JDouble::valueOf(value.deviceId.value()) : nullptr
      );
    }
  };

} // namespace margelo::nitro::thermalprinter
